version: "1"
packages:
  - name: "models"
    path: "internal/app/models"
    queries: "./db/query/"
    schema: "./db/"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: false
